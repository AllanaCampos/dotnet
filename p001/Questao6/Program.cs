using System;
public class Program
{
    public static void Main(string[] args)
    {
        string str1  = "Hello", str2 = "World";
        if(str1 == str2)
            Console.WriteLine ("A string " + str1 + " e igual a string " + str2);
        else
            Console.WriteLine ("A string " + str1 + " e diferente da string " + str2);
        
    }
}